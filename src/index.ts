// wplot — public entry. The core plotting API: createPlot, Plot, series/object
// inputs, events, link groups, and the plugin contracts (Plugin, registerSeries
// /registerObject, the onDraw overlay surface, and the adapter authoring types).
export * from "./lib";
